# Закрытие дня: Statistics, Miss Chips и баланс по всем филиалам

## Проверено

- За 25/08 день закрыт в Arusha, Dodoma, Mbeya и Mwanza; результаты столов и Miss Chips в базе присутствуют.
- `Statistics → Daily` получает данные через `compute_daily_diff`, а `Statistics → Total` собирает их отдельными запросами. Отсутствие `fin_day_balance_snapshot` само по себе не объясняет пропажу строки из Statistics.
- Сейчас знак Miss Chips расходится между источниками: `shifts.miss_total` хранит канонический знак, а `closing_count.chip_miss_total` — противоположный. `MissChips.tsx` и `compute_daily_diff` читают сырой JSON, тогда как Day Closings читает `shifts.miss_total`.
- Фактический результат `compute_daily_diff` за 25/08 существует для всех филиалов. Остаточная разница: Arusha −15 000, Dodoma 0, Mbeya 0, Mwanza +3 030 000 TZS.

## Что исправим

1. **Единый источник Miss Chips**
   - Перевести `compute_daily_diff` и экран Miss Chips на `shifts.miss_total`.
   - Убрать повторное вычисление знака из `closing_count`, чтобы Statistics, Day Closings, печать и Miss Chips показывали одну сумму и одно направление.

2. **Единая формула баланса**
   - Зафиксировать серверную формулу дневного расхождения через канонический `miss_total` без скрытой инверсии в UI.
   - Обновить `Reports.tsx`, чтобы колонка Balance показывала значение, возвращённое сервером, а не пересчитывала его независимо.
   - Проверить и синхронизировать подписи/цвета: отрицательный Miss Chips означает меньше фишек в кассе и больше денег.

3. **Надёжное появление закрытого дня**
   - После ручного Close Day инвалидировать запросы Statistics, Miss Chips, Day Closings и дневного баланса, чтобы закрытый день появлялся без перезагрузки приложения.
   - Проверить границы бизнес-дня 07:00 EAT и использовать `business_date_of`, а не разные клиентские преобразования даты.

4. **Проверка всех филиалов и данных 25/08**
   - Повторно выполнить серверный расчёт для Arusha, Dodoma, Mbeya и Mwanza.
   - Для Mwanza отдельно разложить +3 030 000 на Table Result, кассовые IN/OUT и Miss Chips. Исправлять данные только если будет подтверждена ошибочная проводка; реальное расхождение не маскировать.
   - Проверить строки 25/08 в Statistics, Miss Chips и Day Closings для каждого филиала и подтвердить одинаковые суммы.

## Технические детали

- База: миграция `compute_daily_diff`, использующая `shifts.miss_total` и возвращающая готовый `diff`.
- Frontend: `src/pages/Reports.tsx`, `src/pages/MissChips.tsx`, инвалидация после закрытия в `src/hooks/use-business-day-closure.ts`.
- Проверка: запросы по четырём казино, выборочные тесты формулы и браузерная проверка экранов.
