# Mbeya: исправить открытие фишек и Check

## Что подтверждено

- В открытой смене Mbeya от 08/08/2026 новый набор фишек записан только в `opening_float.closing_chips` на **87 870 000 TZS**.
- Поле `opening_float.chips` осталось пустым, а `totals.chips_tzs` равно `0`.
- Поэтому общий Opening/Expected равен только наличным **9 000 000 TZS**, хотя должен быть **96 870 000 TZS**.
- Сохранённые проверки показывают ровно эту ошибку: Counted **96 870 000**, Diff **+87 870 000**.

## Исправление

1. Обновить текущую открытую смену Mbeya:
   - записать указанные количества по номиналам в `opening_float.chips`;
   - установить `chips_tzs = 87 870 000`;
   - установить `total_tzs = 96 870 000`, сохранив наличные **9 000 000** и остальные части открытия без изменений.
2. Исправить стартовую запись в истории Check этой смены: добавить фишки открытия и заменить Total/Expected с **9 000 000** на **96 870 000**.
3. Пересчитать уже сохранённые проверки текущей смены: добавить **87 870 000** к Expected и убрать ложную разницу **+87 870 000**, не меняя физически введённые Counted и купюры.
4. Проверить после обновления:
   - Opening и Expected показывают **96 870 000**;
   - текущий чек **96 870 000** показывает **Balanced**;
   - в детализации открытия присутствуют все заданные номиналы фишек.

## Технические детали

- Это точечная корректировка данных открытой смены Mbeya и связанных `cash_counts`; схема базы и интерфейс не меняются.
- Начальный справочник `chip_initial_baseline` и кассирский `chip_baseline` уже содержат правильные количества, их повторно менять не нужно.