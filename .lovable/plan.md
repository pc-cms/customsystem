# Закрытие смены Arusha с итогом по столам +8 072 000

## Текущее состояние (проверено в базе)
- Arusha Cloud: одна открытая смена, открыта 30.07.2026 17:52 EAT, `closing_cash` не заполнен.
- Расчёт по столам сейчас даёт 3 350 000 (AR1 +4 900 000, AR2 −1 640 000, BJ1 −60 000, P1 +150 000). Все игровые столы уже закрыты.
- Opening float: 9 000 000 TZS наличными + 177 551 000 фишками.

## Что сделаю
1. Зафиксирую денежную часть закрытия по текущему состоянию системы: `closing_cash` = остаток кассы, рассчитанный из opening float и всех движений смены (транзакции, расходы, коллекции, трансферы), фишки закрытия — по текущему инвентарю. Пересчёт кассира не вводится, расхождение по деньгам = 0.
2. Закрою смену: `status = closed`, `closed_at` = текущее время, с записью в лог действий (причина: ручное закрытие менеджером через систему).
3. Зафиксирую итог по столам `tables_result = 8 072 000` как окончательное значение смены. Разбивка по столам остаётся текущей — расхождение 4 722 000 не распределяется.
4. Пересчитаю `cash_desk_result`, `miss_total` и `balance` от зафиксированного итога, чтобы карточка смены и Cage Closings показывали согласованные цифры.

## Технические детали
- Всё выполняется одной миграцией (закрытие + фиксация значений), т.к. затрагиваются триггерные поля.
- Триггер `compute_shift_balance` / зеркалирование `compute_shift_table_results` при закрытии перезаписывает `tables_result` расчётным значением, поэтому фиксация 8 072 000 выполняется после закрытия, с временным отключением пересчёта для этой строки.
- Реестр столов и снапшоты фишек не трогаю — правится только строка смены.
- Проверю после применения: смена в статусе closed, `tables_result = 8072000`, страница Cage Closings показывает согласованный Balance.
